import 'package:flutter/material.dart';

class TransactionSideMenu extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return Drawer(
      child: ListView(

      ),
    );
  }
}